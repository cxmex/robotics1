{
  "$schema": "https://railway.com/railway.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "cd tutor && python -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
  }
}
